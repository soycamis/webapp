define([
        'jquery',
        'underscore',
        'backbone'
    ], function ($, _, Backbone) {
        'use strict';

        var PlaceModel = Backbone.Model.extend({});

        return PlaceModel;
    });